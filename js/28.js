/* Imports y Exports */

import { 
    sumar as suma, 
    restar 
} 
from "./funciones.js" // el punto quiere decir la ubicacion actual

const resultado = suma(20, 30)
const resultado2 = restar(20, 30)

console.log(resultado)
console.log(resultado2)